#include<bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    string a; getline(cin, a);
    if(a == "DEC 25" || a == "OCT 31") cout << "yup" << endl;
    else cout << "nope" << endl;
    return 0;
}