#include<bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long a;
    cin >> a;
    cout.precision(8);
    cout << 4 * sqrt(a) << endl;
    return 0;
}